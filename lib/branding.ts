/** White transparent logo — tinted sage green via `.logo-header-accent` in CSS. */
export const HEADER_LOGO_SRC = "/logo/logo-header-white.png";

/** Intrinsic pixel size of the processed header logo asset. */
export const HEADER_LOGO_WIDTH = 943;
export const HEADER_LOGO_HEIGHT = 853;

/** Pre-coloured sage logo for transactional emails (CSS filters are unreliable in clients). */
export const EMAIL_LOGO_PATH = "/logo/logo-email-green.png";
